# npm Registry Configuration
# This file configures npm registry for this project only

# Option 1: Use default npm registry (npmjs.com)
registry=https://registry.npmjs.org/

# Option 2: Use custom private registry (uncomment if needed)
# registry=https://your-private-registry.com/

# Authentication token will be set via environment variable or login command
# DO NOT commit authentication tokens to git!

# For scoped packages (if using @your-scope/package-name)
# @your-scope:registry=https://registry.npmjs.org/
